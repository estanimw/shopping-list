# URL pública de la aplicación (sin barra final)
BETTER_AUTH_URL=http://localhost:3000

# Generá un valor aleatorio largo antes de desplegar, por ejemplo: openssl rand -base64 32
BETTER_AUTH_SECRET=cambia-este-valor-por-un-secreto-largo-y-aleatorio

# Opcional. Por defecto se usa data/shopping.db.
# DB_PATH=/app/data/shopping.db
